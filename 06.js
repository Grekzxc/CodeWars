// Напишите функцию, которая разбивает строку и преобразует ее в массив слов.

function stringToArray(string){
    return string.split(' ') // пробел это разделитель между словами, split разбивает строки на массив подстрок
    }